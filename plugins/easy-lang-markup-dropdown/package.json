{
  "name": "easy_lang_markup_tinymce_plugin",
  "version": "1.0.0",
  "description": "Language attribute plugin for TinyMCE, with TypeScript and Jest",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node esbuild.config.mjs",
    "test": "npm run build && jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/tinymce": "^5.5.0",
    "esbuild": "^0.25.5",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.4.0"
  }
}
